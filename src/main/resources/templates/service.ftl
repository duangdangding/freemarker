package ${jsonParam.servicePath};

import com.baomidou.mybatisplus.extension.service.IService;
import ${jsonParam.entryPath}.${tableInfo.className};

/**
* ${tableInfo.className}接口层
*
* @author ${jsonParam.author}
* @version 1.0.0 ${today}
*/
public interface ${tableInfo.className}Service extends IService<${tableInfo.className}> {

}