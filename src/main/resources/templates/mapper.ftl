package ${jsonParam.mapperPath};

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import ${jsonParam.entryPath}.${tableInfo.className};

/**
* ${tableInfo.className}数据接口层
*
* @author ${jsonParam.author}
* @version 1.0.0 ${today}
*/
public interface ${tableInfo.className}Mapper extends BaseMapper<${tableInfo.className}> {
    
}